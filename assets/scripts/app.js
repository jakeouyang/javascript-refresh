// import { apiKey } from './util.js';

// console.log(apiKey);

const hobbies = ["Sports", "Cooking", "Reading"];

const index = hobbies.findIndex((item) => {
  return item === 'Reading';
});

console.log(index)

